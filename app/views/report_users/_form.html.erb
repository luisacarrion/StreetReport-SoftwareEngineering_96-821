<%= form_for(@report_user) do |f| %>
  <% if @report_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_user.errors.count, "error") %> prohibited this report_user from being saved:</h2>

      <ul>
      <% @report_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :report_id %><br />
    <%= f.number_field :report_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
